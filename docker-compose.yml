version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: college_erp_postgres
    environment:
      POSTGRES_DB: college_erp_db
      POSTGRES_USER: erp_user
      POSTGRES_PASSWORD: erp_password_123
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U erp_user -d college_erp_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - college-erp-network

  redis:
    image: redis:7-alpine
    container_name: college_erp_redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - college-erp-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: college_erp_backend
    ports:
      - "5000:5000"
    environment:
    
      NODE_ENV: development
      DB_HOST: postgres
      REDIS_HOST: redis
      PORT: 5000
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend/src:/app/src
      - ./backend/node_modules:/app/node_modules
    command: npm run dev
    networks:
      - college-erp-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: college_erp_frontend
    ports:
      - "5173:5173"
    environment:
      VITE_API_URL: http://localhost:5000/api
    depends_on:
      - backend
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/node_modules:/app/node_modules
    networks:
      - college-erp-network



volumes:
  postgres_data:

networks:
  college-erp-network:
    driver: bridge